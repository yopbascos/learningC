// declare 2 string variables, 1 is hello, 1 is world, concatenate to say hello world

#include <stdio.h>
#include <string.h>

int main(void) {
    char hello[] = "hello ";
    char world[] = "world";

    strcat(hello, world);

    printf("%s\n", hello);

}
